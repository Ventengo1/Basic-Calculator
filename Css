body {
  background-color: #e0ffff; /* Light blue background color for the entire website */
}

.calculator {
  width: 300px;
  margin: 0 auto;
  padding: 20px;
  border: 1px solid #ccc;
  border-radius: 5px;
  background-color: #f0f0f0; /* Gray background for the calculator */
}

input[type="text"] {
  width: 100%;
  padding: 10px;
  font-size: 24px;
  text-align: right;
}

.buttons {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 10px;
}

button {
  width: 100%;
  padding: 15px;
  font-size: 20px;
  cursor: pointer;
  background-color: #f5f5f5;
  border: 1px solid #ccc;
  border-radius: 3px;
  transition: background-color 0.2s ease-in-out;
}

button:hover {
  background-color: #e0e0e0;
}

button.operator {
  background-color: #f5f5f5;
}

button.equal {
  background-color: #007bff;
  color: #fff;
}

button.clear {
  background-color: #ff6347;
  color: #fff;
}

.history {
  display: none; /* Initially hide the history */
}

#historyList {
  list-style: none;
  padding: 0;
}

#historyList li {
  margin-bottom: 5px;
}

.light-theme {
  background-color: #f5f5f5;
  color: #333;
}

.dark-theme {
  background-color: #111; /* Darker background for the entire page */
  color: #fff;
  .calculator {
    background-color: #222; /* Darker background for the calculator */
  }
  button {
    background-color: #333; /* Darker background for buttons */
    border: 1px solid #444;
  }
  button.operator {
    background-color: #444; /* Darker background for operators */
  }
  button.equal {
    background-color: #0055ff; /* Darker blue for the equal button */
  }
  button.clear {
    background-color: #ff4433; /* Darker red for the clear button */
  }
  .history {
    background-color: #222; /* Darker background for history section */
  }
  #historyList li {
    color: #fff; /* White text for history items */
  }
}

#mode-select {
  margin-right: 10px;
}
