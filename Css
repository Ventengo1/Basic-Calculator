body {
  background-color: #e0ffff; /* Light blue background color for the entire website */
}

.calculator {
  width: 250px;
  margin: 0 auto;
  padding: 20px;
  border: 1px solid #ccc;
  border-radius: 5px;
  background-color: #008000; /* Change background color to green */
}

input[type="text"] {
  width: 100%;
  padding: 10px;
  font-size: 20px;
  text-align: right;
}

.buttons {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 10px;
}

button {
  width: 100%;
  padding: 15px;
  font-size: 18px;
  cursor: pointer;
  background-color: red; /* Change button color to red */
}

button.operator {
  background-color: #f0f0f0;
}

button.equal {
  background-color: #007bff;
  color: #fff;
}

button.clear {
  background-color: #ff6347;
  color: #fff;
}

.history {
  margin-top: 20px;
}

#historyList {
  list-style: none;
  padding: 0;
}

#historyList li {
  margin-bottom: 5px;
}

.light-theme {
  background-color: #f5f5f5;
  color: #333;
}

.dark-theme {
  background-color: #111; /* Darker background for the entire page */
  color: #fff;
  .calculator {
    background-color: #222; /* Darker background for the calculator */
  }
  button {
    background-color: #333; /* Darker background for buttons */
  }
  button.operator {
    background-color: #444; /* Darker background for operators */
  }
  button.equal {
    background-color: #0055ff; /* Darker blue for the equal button */
  }
  button.clear {
    background-color: #ff4433; /* Darker red for the clear button */
  }
  .history {
    background-color: #222; /* Darker background for history section */
  }
  #historyList li {
    color: #fff; /* White text for history items */
  }
}

#mode-select,
#theme-select {
  margin-right: 10px;
}
